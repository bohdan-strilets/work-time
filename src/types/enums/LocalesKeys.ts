export enum LocalesKeys {
  common = 'common',
  statistics = 'statistics',
  aboutUs = 'aboutUs',
  profile = 'profile',
  calendar = 'calendar',
  notFound = 'notFound',
  resetPassword = 'resetPassword',
}
